###
💡 سناریوی استفاده و نحوه عملکرد (Use Case)
این اسکریپت با هدف مدیریت هوشمند پروکسی‌های لوکال طراحی شده است. عملکرد اصلی آن بر پایه اولویت‌بندی (Priority-based Load Balancing) است:

اولویت‌بندی هوشمند (Failover): ترافیک ورودی به HAProxy همیشه به اولین سرور سالم در لیست هدایت می‌شود (balance first). 🥇

سوئیچ خودکار: اگر سرور اول دچار اختلال شود، ترافیک بدون هیچ وقفه‌ای به سرور دوم منتقل می‌شود. به محض اینکه سرور اول دوباره در دسترس قرار بگیرد (Health Check موفق)، HAProxy ترافیک را مجدداً به سرور اول بازمی‌گرداند. 🔄

ظرفیت نامحدود: شما می‌توانید هر تعداد سرور که بخواهید برای لودبالانسر تعریف کنید. ♾️

🛠 مثال کاربردی با پنل 3X-UI:
این اسکریپت برای ترافیک‌های داخلی طراحی شده و بهترین راه برای اتصال به سرورهای خارج، استفاده از یک واسط است:

گام اول: در پنل 3X-UI، چند Outbound از نوع VLESS (متصل به سرورهای مختلف) تعریف کنید.

گام دوم: برای هر کدام، یک Inbound از نوع SOCKS بسازید و در قسمت Listen IP مقدار 127.0.0.1 را قرار دهید تا فقط از داخل خود سرور قابل دسترسی باشند و حتما تیک پسورد رو خاموش کنید ساکس نباید یوزر پسورد داشته باشه چون لوکال هست امنه . 🔒

گام سوم: پورت‌های این ساکس‌های لوکال را به اسکریپت بدهید تا لودبالانس شوند.

گام نهایی: حالا پورت خروجی لودبالانسر (HAProxy) را به عنوان یک خروجی جدید در پنل تعریف کنید. 🎯

خلاقیت در اجرا: شما محدود به 3X-UI نیستید! می‌توانید مستقیماً از هسته Xray یا هر ابزار دیگری برای مدیریت خروجی‌ها استفاده کنید و ورودی لودبالانسر را به پنل متصل کنید تا یک اتصال پایدار، پرسرعت و ضد‌قطعی داشته باشید. 🚀

<br>
<br>
نصب سریع 🚀 (Quick Start)
برای نصب و راه‌اندازی سریع، دستور زیر را در ترمینال خود کپی و اجرا کنید:

```Bash

bash <(curl -Ls https://raw.githubusercontent.com/110-ghost/SOCKS5-load-balancer/main/run.sh)
```

🛠 پیش‌نیازها
سیستم‌عامل: اوبونتو ۲۰.۰۴ یا بالاتر.

دسترسی: کاربر root.

پکیج‌ها: haproxy, socat, python3, python3-pip, sqlite3 (توسط اسکریپت نصب می‌شوند).




🔍 راهنمای مراحل نصب (Interactive Install)
هنگام اجرا، اسکریپت سوالات زیر را از شما می‌پرسد:

```
FRONT_IP & PORT: آی‌پی و پورتی که می‌خواهید لودبالانسر روی آن کار کند (حتما از ای پی لوکال 127.0.0.1 استفاده بشه).

Stats Page: پورت، مسیر (URI) و نام کاربری/رمز عبور برای پنل وب HAProxy.

Telegram Bot: توکن ربات و Chat ID ادمین.

Backend Servers: تعداد سرورهای لوکال، نام هر کدام و پورتی که روی آن فعال هستند (مثلاً سرور vless_1 روی پورت 10001).
```



🤖 راهنمای دریافت اطلاعات تلگرام
```
Bot Token: به @BotFather پیام دهید، /newbot را بزنید و توکن را کپی کنید.

Chat ID: @GetChatID_IL_BOT مثلا ازین ربات میتونید چت ای دی خودتونو ببینید 
```


📂 ساختار فایل‌ها و مسیرها
```
/etc/haproxy/haproxy.cfg	فایل پیکربندی اصلی HAProxy
/etc/haproxy/telegram_bot_config.json مشخصات ربات تلگرام و چت ایدی که تعریف میکنید 
/etc/haproxy/check_vless_e2e.sh اسکریپت برسی وضعیت سرور ها
/usr/local/bin/haproxy_telegram_bot.py فایل پایتون ربات مانیتورینگ
/var/lib/haproxy/stats.db دیتابیس ذخیره آمار ۶ ساعته
/etc/systemd/system/haproxy-failover.service سرویس مدیریت خودکار failover
/etc/systemd/system/haproxy-telegram-bot.service سرویس مدیریت تلگرام
/etc/systemd/system/haproxy.service مدیریت سرویس haproxy
```
📊 پنل وضعیت HAProxy
در انتهای نصب، اطلاعات پنل به صورت زیر نمایش داده می‌شود:
```
┌──────────────────────────────────────────────────┐
│             HAProxy Stats Dashboard              │
├──────────────────────────────────────────────────┤
│ URL: http://YOUR_IP:STATS_PORT/STATS_URI         │
│ User: YOUR_USER                                  │
│ Pass: YOUR_PASS                                  │
└──────────────────────────────────────────────────┘
```
